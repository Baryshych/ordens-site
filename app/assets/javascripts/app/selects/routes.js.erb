module.config(['$stateProvider',
  function($stateProvider, $urlRouterProvider) {
    $stateProvider
      .state('selects', {
        url: '/selects?per_page',
        templateUrl: "<%= asset_url 'selects/index' %>",
        controller: 'SelectsCtrl'
      })
}]);
